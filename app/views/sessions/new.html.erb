<% provide(:title, "ログイン") %>
<div class="main login">
    <div class="form">
        <h1>ログイン</h1>
        <%= form_for(:session, url: login_path) do |f| %>
        <div>
            <%= f.label :email,"メールアドレス" %>
            <%= f.email_field :email, class: 'form-under-line' %>
        </div>
        <div>
            <%= f.label :password, "パスワード" %>
            <%= f.password_field :password, class: 'form-under-line password_form' %>
        </div>
        <div>
            <%= f.label :remember_me, class: "checkbox inline" do %>
            <%= f.check_box :remember_me %>
            <span>ログイン状態を保持する</span>
            <% end %>
        </div>
        <div class="submit">
            <%= button_tag do %>ログイン<% end %>
        </div>
        <% end %>
        <p style="margin-top:10px;">パスワードを忘れた方は<%= link_to "こちら", new_password_reset_path %></p>
        <p style="margin-top:10px;">ユーザー登録が済んでいない場合は<%= link_to "こちら", signup_path %>から会員登録</p>
    </div>
</div>
